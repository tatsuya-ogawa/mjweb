[project]
name = "export-scenes"
version = "0.1.0"
description = "Export mjlab task scenes for the mjweb web app."
requires-python = ">=3.12,<3.14"
dependencies = [
    "mjlab",
    "scipy>=1.17.1",
]

[tool.uv.sources]
mjlab = { path = "../../mjlab", editable = true }
